#!/usr/bin/env python3


def points(filename):
	return None;

def speed_threshold(track):
	return 1; # m/s, 3.6 kmh

def max_pause_time(points):
	return None;

def analyse(record):
	P=points(record.origin());
	min_hole_length=max_pause_time(P);
	step=min_hole_length/2;
	for 
	
