[package]
name = "nano-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
midir = "0.11.0"
ndk-context = "0.1.1"
log = "0.4"

[target.'cfg(target_os = "android")'.dependencies]
libc = "0.2"

