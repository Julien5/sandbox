XNAME:=common
XINCLUDE += -I$(DIREMBEDDED)/$(XNAME)
XLIBS += -L/tmp/build/devhost/$(XNAME)/ -l$(XNAME)

ifndef SRCSDIR
include ../settings
include settings
NAME:=$(XNAME)
SRCSDIR := $(DIREMBEDDED)/$(NAME)
include build.mk
endif

