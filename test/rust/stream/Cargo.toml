[package]
name = "stream"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = {version = "0.12.2", features=["stream"]}
reqwest-streams = {version = "*", features=["json"]}
futures = "0.3"
tokio = { version = "1.27", features=["full"]}
serde = "*"